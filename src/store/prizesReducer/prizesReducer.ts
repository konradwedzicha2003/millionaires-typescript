import {prizes} from "../../data/prizes/prizes";

const prizesReducer = (state = prizes, action: {type: string}) => {
    switch (action.type) {
        default:
            return state
    }
}

export default prizesReducer