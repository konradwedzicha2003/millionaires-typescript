import {questionAndAnswers} from "../../data/questionsAndAnswers/questionsAndAnswers";

const questionsAndAnswersReducer = (state = questionAndAnswers, action: {type: string}) => {
    switch (action.type) {
        default:
            return state
    }
}

export default questionsAndAnswersReducer